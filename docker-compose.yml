name: "paytract"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: "3000"
      EXAMPLE: ${EXAMPLE}

    ports:
      - "3000:3000"
    restart: unless-stopped
